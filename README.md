# MasterHub
## MasterHub is the perfect tool for all tasks!
### --Perquesties--
There are multiple prequesties if not installed via the installer.
To install these prequesties, use the following commands in the terminal:
```
pip install customtkinter
pip install darkdetect
pip install Decimal
```
The second command is only required if customtkinter doesn't install it automatically.
abcd